<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.michaelsinner.sabergo">

    <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <!-- Manifest Activty Main -->
        <activity
            android:name=".MainActivity"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Manifest Activty Index -->
        <activity android:name=".Activities.Index">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <!-- Manifest Activty Login -->
        <activity android:name=".Activities.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <!-- Manifest Activty Main Menu -->
        <activity
            android:name=".Activities.MainMenu"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <!-- Manifest Activty Tutorial -->
        <activity
            android:name=".Activities.Tutorial"
            android:label="@string/title_activity_tutorial"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".Activities.MainMenu"
            />

        <!-- Manifest Activity Acerca de -->
        <activity android:name=".Activities.About"
            android:theme="@style/AppTheme01"
            android:parentActivityName=".Activities.MainMenu">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Manifest Activity Logros -->
        <activity android:name=".Activities.Achievements"
            android:theme="@style/AppTheme01"
            android:parentActivityName=".Activities.MainMenu">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Manifest Activity Compartir-->
        <activity android:name=".Activities.Share"
            android:theme="@style/AppTheme01"
            android:parentActivityName=".Activities.MainMenu">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Manifest Activity Configuraciones -->
        <activity
            android:name=".Activities.Settings"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".Activities.MainMenu"/>
        <activity android:name=".Activities.PruebaDiagnostico" />

        <!-- Manifest Activity Registrarse -->
        <activity android:name=".Activities.SignUp"
            android:label="Registrarse"
            android:theme="@style/AppTheme01"
            android:parentActivityName=".Activities.Login">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity android:name=".Activities.ModuloAR" />

        <!-- Manifest Activity Perfil -->
        <activity
            android:name=".Activities.Profile"
            android:label="@string/title_activity_profile"
            android:theme="@style/AppTheme01"
            android:parentActivityName=".Activities.MainMenu">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Manifest Activty Módulo Pruebas Diarias -->
        <activity android:name=".Activities.ModuloPruebasDiarias"
            android:theme="@style/AppTheme01"
            android:parentActivityName=".Activities.MainMenu">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

    </application>

</manifest>